#A palindromic number reads the same both ways. 
#The largest palindrome made from the product of two 2-digit numbers is 9009 = 91 × 99.
#Find the largest palindrome made from the product of two 3-digit numbers.

import itertools
def palindrome_multiplyer ():

#two 3-digit numbers, a and b
#want to find all products in range 100-999
#want to store that value




def find_multiples (num1, num2):
	start1 = num1
	start2 = num2
	number_products = []
		while start1 and start2 < 100
			product1 = start1*start2
			
	#intertools? need to iterate through multiple variables, 
	#but variables don't need to be in lockstep
	
	#while a and be are both below 100, keep adding N+1
	
	


def palidrome_finder ():
	palindrome_products = []
	for x in number_products:
		if string[x] ==
			palindrome_products.append(int(x))
		return palidrome_products
		else 
			return False
			
def max_palidrome ():
	return max(numbers)

